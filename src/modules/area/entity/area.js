class Area {
  constructor({ id, name, createdAt, lastUpdated }) {
    this.id = id;
    this.name = name;
    this.createdAt = createdAt;
    this.lastUpdated = lastUpdated;
  }
}

module.exports = Area;
